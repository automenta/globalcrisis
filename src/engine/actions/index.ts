export * from './actionTypes';
// BaseAction.ts is not created as IAction interface in GameEngine.ts is sufficient for now.
// If actions need their own methods in the future, a BaseAction class could be introduced.
